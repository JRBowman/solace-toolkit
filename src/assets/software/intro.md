# Overview
```typescript
let description = "check out the interesting software concepts and source that I publish from day-to-day!";
```
```
// TODO: Add topics and content generation from .md in git: [t-02] - *ngFor="let i of sArticles" => generate content pages for each article markdown.
```